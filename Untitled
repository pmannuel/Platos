if day == 'mon':
    mon = Schedule.objects.filter(user_id = user_id)
    exclude = ""
if day == 'mon':
    mon = Schedule.objects.filter(user_id = user_id)
    exclude = ""
if day == 'mon':
    mon = Schedule.objects.filter(user_id = user_id)
    exclude = ""
    i = 0
    while i < 24:
        check = 'mon[0].mon.h' + str(i) + 'to' + str(1+1)
        if not check =
        exclude += ".exclude(" + check + ")"
